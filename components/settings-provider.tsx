"use client"

// SettingsProvider is now a no-op - CSS variables are no longer dynamically applied
// This component is kept for backwards compatibility in case it's imported elsewhere
export function SettingsProvider() {
    return null
}
